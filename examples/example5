<-- This program calculates 3^4-->

num power(num a, num b).

num main(){


	printf("The answer to 3^4 is %d\n", power(3,4)).

}


num power(num a, num b){
	when(b == 1){
		return a.
	}
	return a * power(a, b - 1).
}
